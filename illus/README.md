# Site illustrations

One 1400 x 781 JPEG per page. Every illustration is used exactly once: never reuse a file on a second page.
Files are wired into pages by `scripts/illustrations.mjs` at build time, and only when the file exists here.
